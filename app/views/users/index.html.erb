Hi!
Search for a user 
<%= link_to "Sign in", new_user_session_path%>
<%= link_to "Create New Account", new_user_registration_path%>
<%=button_to('Logout', destroy_user_session_path, :method => :delete)%>

<%=form_with model: @user, method: :get, data:{turbo:false} do |form|%>
<%=form.label :full_name, "Full Name"%>
<%=form.text_field :full_name%>
<%=form.submit "Search"%>
<%end%>

Users Found:
<%if @users_found%>
    <%=@users_found.each do |user|%>
    <%= user.full_name%>
    <%=link_to "View Profile", user_path(user), id: user.id%>
    <%end%>
<%end%>

Friend Requests:
<%if current_user.pending_friend_requests.empty?%>
No new friend Requests
<%else%>
    <%current_user.pending_friend_requests.each do |friend_request|%>
    <%=friend_request.sent_from.full_name%>
    
     <%=form_with model:@friendship, data:{turbo:false} do |f|%>
     <%=f.hidden_field :friend1_id, value: friend_request.sent_from.id %> 
     <%=f.hidden_field :friend2_id, value:current_user.id%> 
     <%=f.submit "Accept Friend Request"%> 
     <%end%>




    <%end%>
<%end%>
<br/>
CURRENT USER <%=current_user.full_name%>
<br/>

Pending friends:
<%if current_user.fr_sent.empty?%>
No friend requests awaiting response 

<%else%>
    <%current_user.fr_sent.each do |fr|%>
    <%=fr.sent_to.full_name%>
    <%end%>
<%end%>