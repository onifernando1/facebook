Hi!
Search for a user 
<%= link_to "Sign in", new_user_session_path%>
<%= link_to "Create New Account", new_user_registration_path%>
<%=button_to('Logout', destroy_user_session_path, :method => :delete)%>

<%=form_with model: @user, method: :get, data:{turbo:false} do |form|%>
<%=form.label :full_name, "Full Name"%>
<%=form.text_field :full_name%>
<%=form.submit "Search"%>
<%end%>

Users Found:
<%if @users_found%>
    <%=@users_found.each do |user|%>
    <%= user.full_name%>
    <%=link_to "View Profile", user_path(user), id: user.id%>
    <%end%>
<%end%>